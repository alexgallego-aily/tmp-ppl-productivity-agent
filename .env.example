# Aily environment configuration for local development.
# Loaded automatically by AilySettings via python-dotenv.
# Copy to .env and fill in your values:  cp .env.example .env

# ── Core Aily settings ────────────────────────────────────────────
AILY_PREFIX=aily
AILY_ENV=ai
AILY_TENANT=sanofi
AILY_CLOUD_PROVIDER=aws

# ── AWS settings ──────────────────────────────────────────────────
AWS_REGION=eu-central-1
AWS_PROFILE=your-aws-profile-here
